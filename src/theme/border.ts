import type { ThemeColors } from "../generated/theme-colors.js";
import { semantic } from "../semantic.js";

export const borderColors = {
    "focusBorder": semantic.zero,
    "contrastBorder": semantic.zero,
} satisfies ThemeColors;
